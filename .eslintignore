# /node_modules/* and /bower_components/* ignored by default

# Mocha is best written in ES5, so ignore mochaPassing arrow 
# functions to Mocha is discouraged. Their lexical binding of 
# the this value makes them unable to access the Mocha context,
# and statements like this.timeout(1000); will not work inside 
# an arrow function.

./test/*